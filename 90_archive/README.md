# 90_archive — 各 session 原始素材（只读）

大文件**不进 git**（已在 .gitignore）。每个 session 目录放一份 `_manifest.md`：

```
文件名 | 大小 | 来源(S3-A…) | 用途(Step) | 落地路径 | 是否已派生进 10_product/40_images
```

这样即使原素材丢了，下一个 session 也知道"曾经有什么、被我转化到哪去了"。
