# Hermes 开工页

2026年9月25日。Session 10 的合并、筛选、瘦身和分析，在授权范围内已经结束。后续由 Hermes 继续。不要重做下面已完成的瘦身。

现行分支是 `arena/01a0cc8f-retrofitlock`。main 仍是旧基线，不要合并，除非用户这一句明确写“合并到 main”。不要 force-push，不要改写已推送提交。

## 先读这几页，不要重读全文

1. `00_meta/METHOD_CURRENT.md`：现行闸门。
2. `00_meta/AI_NOW.md`：AI 已做完的最短结论，含 20 题边界和线索画像。
3. `00_meta/CONFLICT_LOG.md`：已覆盖的冲突，以及仍开着的缺口。
4. `00_meta/NEXT_STEP.md`：瘦身之后停在哪里。
5. `00_meta/INSTALLER_SENTENCE_CHECK.md`：页面上允许说的句子。
6. `00_meta/DRIVE_NAME_TREE.md`：云端文件名树。没有链接，没有标识。
7. `00_meta/TOKEN_SAVE.md`：不要整夹重读，不要猜入口。

Drive 父夹入口不在 Git。它只可能在本地忽略文件。那个文件若不在，只报告入口不在，不猜，不重列，请用户重给。不要把链接或标识写进 Git 或公开答复。

## 不要重复做

- 不要再按时间覆盖已经收进冲突日志的旧架构、旧开工门、旧唯一数据源。
- 不要找回灰黑图或灰黑图记录。出图只打开 CAD 日志第一节和旋钮日志第一节。云端原件不删。
- 不要重读已经读过的英中目录、三份执行说明、90 天四步任务、渠道排序笔记。用 `AI_NOW.md`。
- 不要重读合格线索、商业 ICP、集成商英文表来抄联系人、电话、邮箱或公司名。
- 不要把 536、129 和 8 月富化快照相加，也不要把它们写成客户总数或发送批准。
- 不要把两个系列夹的夹名改成产品规格。夹只列了名，没有打开。
- 不要把 AccessX 写成成品或对外品牌，不要写成已接西门子。
- 不要选定品牌。封面仍是 Name pending。
- 不要外发，不要公布占位邮箱，不要把星号性能写成已验证事实。
- 不要把工程站、安装商草稿、未建的公益站合成一站。
- 不要把新框架站上的“原钥匙始终保留、十分钟安装、色板、当天放行、成品 App”抄进页面。
- 114、124、125、126、128 上次列表没见到。没看到就报告没看到，不编正文。
- 证书正文没读。不写已认证。

## 已留下、可直接用

- 安装商草稿：`60_website/index.html`、`catalog.html`、`markets.html`、`questions.html`。
- 20 题纯文本：`60_website/buyer-questions.txt`。这是归纳筛选，不是旧文件原样抄来的。
- 一个驱动核心，两条路径。S1 保留现有锁芯。S2 更换欧标锁芯。
- 外壳记录只在 CAD 日志第一节。包络可以标成 CAD 记录，不是证书。

## Hermes 可以新做的

扩大潜在客户是下一件工作，不是本会话瘦身的未完成项。做的时候仍不外发，不抄联系人，不把线索分数当成采购。先列文件名，再读用户点名的那一份。本地客户画像、异议和话术文件若仍是待导入页，不要编买家原话。

## 仍开着，不是没瘦完

系列图像素没打开。证书正文没读。联系人许可不公开。没见到的编号不编。这些保持开着，直到用户单独点名。
