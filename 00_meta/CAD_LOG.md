# CAD_LOG — CAD 唯一汇总（2026-09-20 R16 合并 / R17 轮廓原文复核）

> 所有 CAD 相关内容合并到本文件；`PRODUCT_TRUTH` §1ab/`DRIVE_INDEX` §4/`REUSABLE` 数字段均已指向这里。
> **用户裁定（R16）**：外观图只保留外壳外观尺寸；内部/工程数字（face/edge/vertex、local bbox、体积、
> placements、Slider 边、15.18mm³ 干涉等）**已从仓库删除**——原件在 Drive `107/111`，升 Tier 1 时再取。
> **R17（用户纠正 + STEP 原文复核）**：轮廓**不是**「跑道形/两端半圆」。已按 `sx-db-4.stp` 原文核实：
> **只有上端（圆盘端）为完整半圆 R19.9；下端（电池盖端）= 平直边 + 两侧 R≈4.8 圆角**。
> 「跑道形」是我未核原文的推断，且 `QA_ledger` 旧错「跑道形曾被用户否决」未去查 → 已录 §4 干扰台账，prompt 禁该措辞。

## 1. 外观尺寸账本（出图 prompt 唯一允许引用的 CAD 数据）
```text
包络      39.800 W × 22.500 D × 90.500 H mm（源：sx-db-4.stp；R17 原文自洽核验：19.9+65.6+5.0=90.5）
轮廓(R17) 上端（大圆盘端）= 完整半圆 R19.9（直径=满宽 39.8）
          中部直段      = 宽 39.8 × 高 65.6
          下端（电池盖端）= 平直横边（边宽 29.8）+ 两角 R≈4.8 圆角过渡到直边
          —— 只有一端是半圆；不是跑道/胶囊形；下端不是半圆，下角也不是直角
正拍比例  宽/高 = 0.44（39.8/90.5）；25° 三视剪影宽/高 ≈ 0.50（QA 容差 ±6%，PIL 量剪影 bbox）
厚度观感  深 22.5mm；25° 视角只露一条薄侧面，整体"纤薄竖块"；禁 chunky/thick 一类词
壳材质    喷砂阳极氧化铝；颜色=浅银灰（与 CAD 渲染一致，A01"氧化灰/黑"为历史方向，待用户终审）
          前口一圈金属抛光亮边（O1）
面盖      闭合状态、齐平、棕红细密纹理=普通塑料（O6；不是木头；不得皮革感）
上端      大圆盘（近满宽，随顶部半圆）内含 斜向·浅色·扁平·微凹 长条 = 手动旋钮区（O2 用户终审；
          旧"近直角矩形长条 4.30:1"规则作废，凸起把手/金属套禁止）
盘中下    小浅灰圆点 = LED（O3 暂定，对外文案不写）
中部      圆形棕色按键 = 功能键（O4 用户确认）
中下      LOGO 刻字 → 出 A（带）/ B（不带，one-pager 用）两版（O5 用户确认）
底部      电池盖 = 同棕色面盖材质、齐平小区域、恰 3 道细浅横纹（O8 用户终审"就是电池盖"；
          FCStd 内部加强筋不外露，不作外观依据；用户已说不再需要 3 张装配截图）
禁止      背面/侧面底部/接口/内部件一律不画不猜（O9：确认稿=单张 3/4 视角，其余不可见）
          原稿混入的 CAD 坐标轴，底稿使用时清除（O7）
```

## 2. 确认稿 O1–O9 比对清单（每张生成图出图前逐条对；出图后逐条打钩）
| # | 项 | 正确描述 | 依据 |
|---|---|---|---|
| O1 | 壳轮廓+亮边 | 上端完整半圆 / 直段 / 下端平直边 29.8 + R4.8 角；前口一圈金属抛光亮边 | R17 `sx-db-4.stp` 原文复核 |
| O2 | 旋钮 | 盘内 斜向·浅色·扁平·微凹 长条；非凸起把手、无金属套 | 用户终审 |
| O3 | LED | 盘下小浅灰圆点（暂定，对外文案不写） | 确认稿 |
| O4 | 功能键 | 中部圆形棕色按键 | 用户确认 |
| O5 | LOGO | A 带 / B 不带（one-pager 用 B） | 用户确认 |
| O6 | 面盖纹理 | 棕红、细密近无纹塑料；非木、非皮革感 | 用户终审 |
| O7 | 坐标轴 | 原稿混入 CAD 坐标轴，须清除 | 确认稿 |
| O8 | 底部盖 | 齐平小区域 + 恰 3 道细浅横纹；非深凹槽、非格栅 | 用户终审 |
| O9 | 可见范围 | 单张 3/4 视角；背面/侧底/接口不臆造 | 用户终审 |

**比对权威图**：Drive `确认设计稿-闭合面盖-带Logo字.png`（id `11XTS3AWTVC2bBLY__Vrcca3sYuzmbqPt`，folder `1MHsQ5cQTBeUyh-eS3rv-qMfk4Bf8SnhT`，359KB）。
现状：沙盒无法取得 png 像素（TLS/500/附件不落盘，见 `DRIVE_INDEX` §5）→ 比对按上表 + 用户五终审记录逐条打钩；像素级对齐待通道裁决（默认=肉眼级）。
每图自检 = §1 全部行 + O1–O9 逐条 + 数字剪影比例；两轮不过 → 降级提问，不得循环（session4 教训）。

## 3. 现行裁决（CAD 侧，一行版；历史全量在 Drive `111/112/113` 与编号 md）
- 外观唯一依据 = CAD 铝壳外形（§1，R17 已按原文复核）+ 确认稿（用户铁律 09-20）；历史冻结链全 superseded：
  R19/R20 冻结图、ImmutableCrop_v2（含 100/100）、31/33 provisional、Hero 派生、Registry §10、66 旋钮裁剪。
- 109 = rejected-by-user（电池盖应底部、被贴到顶部圆弧端）；97–100 渲染链、71–93 底部塑料推断、v4 旋钮 = 禁止引用。
- 像素 100/100 ≠ 用户批准：只证明"忠实于当时底稿"。
- 组合规则（94）：成品图只允许 真实铝壳 + 确认面盖 + 确认旋钮 + 唯一底部电池盖；内部件不得出现。
- 禁止从视觉稿反推厚度/R 角（94）；外壳尺寸只认 `sx-db-4.stp` 的包络（§1 第一行）。

## 4. 干扰台账（信息泄进"外观生成"的记录 → 出现即删条目，Tier 1 需要时去 Drive 重取原文）
| 干扰源 | 怎样带偏了图 | 处置 |
|---|---|---|
| **我未核原文的推断「跑道形/两端完整半圆」**（R16 合并时凭印象写入；`QA_ledger` 旧错「锁形改成跑道形→用户判不能锁定」未被查阅） | v3 画成两端皆圆，用户裁定"只有一端为半圆" | **R17 已按 `sx-db-4.stp` 原文复核**（上端 R19.9 半圆 / 下端平直边 29.8 + R4.8 角 / 19.9+65.6+5.0=90.5 自洽）；仓库内 2 处「跑道/stadium」已改正；prompt 禁用「stadium/pill/两端圆」措辞；**规则补强：外形描述必须能指到原文证据，无证据=不写** |
| `107/111` 内部数字（bbox/体积/faces/placements/Slider/干涉） | 注意力从"像不像确认稿"滑向"装配对不对"，prompt 混入工程词 | **已删出仓库**；仅 Tier 1 重取 |
| `66/102/94` 旧旋钮规则「近直角矩形长条 4.30:1」 | v1/v2 旋钮=同色凸起把手 | 被 O2 裁决覆盖；prompt 禁用该措辞 |
| `A01`「氧化灰或氧化黑」+ `31` 黑色样机图（只许当相机参考） | v1/v2 壳渲成深炭黑 | 壳色=浅银灰方向（待终审）；"31 仅作构图"写死 |
| `101/105`「plastic matte / frosted shading」措辞 | v1/v2 纹理渲成明显皮革粒 | 改按 O6「细密近无纹」 |
| `107` 零件描述「平板+4 条加强筋·滑动装配」 | v2 底部盖 4 条深凹槽、喇叭格栅感 | 外观只按 O8「3 道浅横纹」；筋=内部特征不作外观依据 |
| 我 prompt 自加的强调词（chunky / noticeably thick） | v1 剪影 0.399/0.376 超差重跑 | 数字进 §1，措辞禁自造 |
> **规则**：出图 prompt 只允许引用本文件 §1/§2；任何其它 md 的措辞/数字进入 prompt = 违规，
> 记入本表并当轮从该文件删除或降级为指针（R16 已照此清理 PRODUCT_TRUTH/DRIVE_INDEX/REUSABLE/QA）。

## 5. Tier 1（工程级）入口 —— 仅当用户明确要"工程级/规格表/装配图"时启用
- 重取 Drive 原文（文本，我方可自读）：`105`（9 步+8 视图+A/B 规则）、`107`（七步工序+数值）、`101`（闸门+旧错 6 条）、`94`（10 硬规则）、`111`（summary）、`102`（双图闸门+清理 6 步）、`09_VisualMethod`（Stage0–6+100 分制）。
- 几何文件（沙盒不可达，登记在案）：`sx-db-4.stp` 222KB 文本（`1kcUb22aSBNMmsP5p2J7B8XdltalI7R8l`，**R17 已复核轮廓相关实体（前墙/两端端盖/主轮廓圆柱面）：上端 R19.9 半圆、下端平直边+R4.8 角**）、
  `sx-dc-gai-4.stp` 130KB 文本（`14DuUTk1ZIi0xwA-Mn8p9op72zLBy3STV`，头+首块已验与 107 一致）、FCStd 子文件夹 `1iGXjZ4djXxYbSHr51WQdcsblDixtRp-B`。
- 渲染栈配方：`capacity.md` §4c（OCP + libGL stub 已打通）。
- 隔离的开放问题（**不阻塞外观图**）：15.18mm³ 配合/干涉、面盖壁厚/固定方式/拆卸、B 版无 LOGO 确认稿、像素通道。
