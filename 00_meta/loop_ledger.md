# LOOP LEDGER — 每条消息一行（M9 的落地处）

用途：证明"每条 message 都进了方法论 loop"是**事实**而不是态度。
规则：我每收到一条你的消息，就先在这里加一行再干活；没有增量也必须写明原因。
列：`轮` = 你第几条消息 · `输入要点` = 你实际说了什么（不是我以为的） · `沉淀` = 新增/更正的方法论 ·
`改了什么` = 文件 · `撤回` = 我上一条说错的东西。

| 轮 | 输入要点 | 沉淀 | 改了什么 | 撤回 / 更正 |
|----|----------|------|----------|-------------|
| R1 | 复述 S1–S4 各干了什么；问我需要什么文件才能接上 S5；问交接方式（图→目录→网站） | `cross-session-persistence-rule`、`no-ai-from-text-for-product-shots`、`one-source-of-truth`、`single-host-multi-accessory-series`（来自你回答） | 建 `00_handoff/`、`10_product/`、`40_images/slots.csv`、README 指向 HANDOFF | 无 |
| R2 | 「你每次都犯这个错误」；要顶层方法论优先、要 index 让我自选、要容量/空间 loop、**不许擅自 merge**、要 S1–S4 全量索引 | `tmp-is-not-durable`、`probe-before-promise`、`cjk-font-via-npm-not-system`、`sandbox-pdf-limitation-workaround`、`video-to-frames-for-refs`、`one-html-two-outputs`、`stage-gate-without-merge` | 建 `00_meta/`（META/naming/methodology/capacity/index/intake/scripts）+ `.gitignore` 挡大素材 | 撤回"把 Session 3 细节当重点推进"：改为顶层规则先行 |
| R3 | 指出"mark as successful"点错、实际没成功；**Drive 链接在别的 session 里一直能用**；说已上传 session3 的 md；追问是否还要企业资料与 S1/S2 索引；质问每条消息是否真进了 loop | `two-network-paths`、`verify-artifact-not-status-label`、`self-export-at-source`、`drive-direct-link-intake` | 更正 `capacity.md` 第 4 节（两条网络）；`META.md` 加 M8b（Drive 直链配方）+ M9（台账）+ M8 改为双通道；`index_sessions1-4.md` 送达方式更正；新增 `verify-upload.sh` 与本台账 | **撤两条错的**：① 我写"Drive 不通、别发链接"→ 错，沙盒 curl 不通 ≠ 工具读不到（对照实验：`fetch_page` 能读 example.com 与 Google 域名）；② 我写"外部素材只能靠聊天附件或 git"→ 已删 |
| R6 | 你给了 Canonical folder（CAD+视频在里头），并暴露出我漏读了 J90（31+ 条方法论 delta 早已存在） | 未新增方法，改为**继承**：新建 `CATALOG_METHOD.md`（8 步 + 9 页骨架 + D45 措辞）；`META.md` 写入项目自己的 **D44 元规则**（先查已有→引用更新→缺失才新增，delta 续编 D56+） | `00_meta/CATALOG_METHOD.md` 新建；`PRODUCT_TRUTH.md` 加 §3b 对外措辞 + §5b 目录状态；`_READ_LOG.md` 更新 J90 已读；`.gitignore` 放行 `90_archive/cad/*.zip` | 撤回我的"另起一套方法论命名"：改为沿用 `GEN/SL + Dxx` 体系；并更正：目录不是"重新设计"而是执行既有 B15/B16 计划 |
| R8b | 自检回环：verify-upload.sh 连出两次假阳性（先误收平台 coding.*，后误收我自己写的仓库模板）| 无新增方法（并入 verify-artifact-not-status-label）| 兜底扫描加 -not -path */RetrofitLock/*；脚本头写明两次假阳性 | 教训：**校验脚本本身必须被校验**，否则"证据"是新的错误来源 |
| R9 | ①`image-1.png`→改名`确认设计稿`，已传 Drive，问我有没有长期保存链接 ②问我有没有从 session3 文件里挖出长期链接 ③–⑦ 五个【问】全部答复（O2 旋钮区✓/O3 暂定LED/O4 功能键✓/O5 出带与不带 LOGO 两版/O8 就是电池盖✓）④指出我流水线缺"先查旧记录"环节 ⑤**永久：绝不提前 merge**，网站也用本分支映射 Cloudflare Pages | 新增 `consult-before-build`（累计 22）；把"能力不记忆"扩到通道选择 | **新建 `00_meta/REUSABLE.md`**（可复用清单：D64≥95/100 阈值、D66 管线、`ImmutableCrop_Extract_v2.py`、one-pager v0.1 等 20 项 + 我白做的 6 项）；`DRIVE_INDEX.md` 新增 §0 原样链接+出处+设计稿待登记；`PRODUCT_TRUTH.md` 新增 §1a 你的五条确认（O8=电池盖→**不再要 3 张截图**；O5→新增 logo AB 图位；O3 标暂定）；实测并落地 **GitHub inbox 通道**（`push-inbox.sh`/`pull-inbox.py`，sha256 往返一致）；M3 写入永久不 merge + Cloudflare Pages 分支映射 | 我自己三处：① push-inbox.sh 缺 git identity（`Author identity unknown`）已修；② pull-inbox 路径需带 `inbox/` 前缀（我照文档写导致 404）已修文档；③ 我上轮"Drive 二进制普遍不可读"→ 已改为按沙盒探测，并新增**第三条更可靠的通道** |
| R9b | 自查（无新输入）：修完规则后复查上一条命令 | 无新增；把 `never-edit-docs-via-bash-strings` 从"提醒"升级为 META **硬约束** | `methodology.md` 的 `consult-before-build` 条目被 shell 吃掉文件名 → 用 edit_file 修复并补记事故；`META.md` M7 加"改文档只准 write_file/edit_file" | 记：`REUSABLE.md` 差点被当重定向目标写坏（因目录权限拒绝而幸免）→ 说明**静默破坏**是真实风险，不是我多虑 |
| R8 | ①旧 session 都能从 Drive 读 CAD，查记录并对比你为什么不能 ②铁律：最新设计师稿 + CAD 铝壳，历史冻结全部有问题，重做 truth/底稿相关 ③附 image-1.png（设计师稿） | 新增 1 条：sandbox-capability-probe-not-memory（累计 21）；其余全部并入项目既有 D57/D58/D64/D65/D66，不另立 | 新建 probe-drive.sh（每次开局测能力）；CAD_LOG.md 整页重写（R-1…R-25 现状列 + 最新稿可见特征 O1–O9 与【问】+ 能力对照表）；PRODUCT_TRUTH.md 新增 §0 铁律并把 §1 五处外观项标 superseded；QA_ledger 加 K9/K10；CATALOG_METHOD 按 D61 改为先做 one-pager；naming 加 superseded 状态；capacity 加 §4b 沙盒差异表；README 顶部挂铁律横幅；我前几轮 2 张 AI 基线图判废待删 | 撤我的两条：① 我把本沙盒 000 写成 Drive 普遍不通 → 真相是**每个沙盒策略不同**（旧沙盒能 curl 拉 CAD）；② 我沿用 R19/R20 冻结底稿与 100/100 评分当依据 → 你判定**全部有问题**，已整体降级 superseded |
| R7 | ①能否自己从 Drive 读那 2 个 CAD 文件 ②把已给文件里 CAD 相关的历史/分析/规则单独整理一份 ③我是否有 Session 3 全量 index，从 CAD 出发挑旧文件 | 无新增方法：本轮全部收敛到项目既有 **D64/D65/D66**（引用而非另立）；delta 续编序号更正为 **D68 起** | 新建 `00_meta/CAD_LOG.md`（R-1…R-25 + 视觉谱系 + Drive 实测）与 `00_meta/INDEX_session3_files.md`；`DRIVE_INDEX.md` 补子文件夹 3 个 id 与实测结论表；我前几轮的 2 张 AI 基线图降级 placeholder 并记 D65 违规；`LOOP_AUDIT.md` 成为 loop 唯一清单 | **两处自我更正**：① 我以为 delta 只到 D55，实为 D67（J90 全文读完才发现）；② 我原计划先做 9 页目录骨架，与项目 **D61**（先做一页 one-pager，不做大而全 Catalogue）冲突 → 按 D61 改；另：`never-edit-docs-via-bash-strings` 本轮再次触发（反引号被当命令替换，一处补丁静默丢失），已用文件编辑工具重做 |
| R5 | ①设计图：电池盖/铝框**从未成功**，反复犯错 ②目录重做但**继承做目录的方法论** ③给了 Drive folder 链接 | 新增 `cad-verified-render-pipeline`、`failure-ledger-before-retry`（累计 20 条）；采纳项目原有状态词表（`naming.md` §4 映射） | 新建 `00_meta/PRODUCT_TRUTH.md`（冻结事实+禁止事项+交换协议）、`00_meta/intake/_READ_LOG.md`（两个 folder 全清单）、`40_images/QA_ledger.md`（K1–K8 可判定式）、图输出拆分 `CAD_Verified/` 与 `Hypothesis_Not_ProductTruth/`；README 顺序加 0 号必读 | **撤回我自己的两条限制结论**：① "Drive 链接不可用"→ 实测 `fetch_page` 能枚举 folder、能读 md 正文（正是项目 `00_EXP1-J09` 早就写好的方法）；② "需要企业资料清单"→ 冻结事实已在 Drive，不需要你重述；改为只要 4 个二进制资产 |
| R4 | 本轮：①"Mark as successful" 点错、实际没成功 ②Drive 在我其它 session 一直能用 ③已传 session3 的 md，问还要不要企业资料与 S1/S2 索引 ④每条 message 是否真进了 loop | 新增 `two-network-paths`、`drive-direct-link-intake`、`verify-artifact-not-status-label`、`never-edit-docs-via-bash-strings`（累计 15 条有效方法） | 更正 `capacity.md` §4；`META.md` M8 双通道 + M8b 配方 + M9 台账；`index_sessions1-4.md` 送达方式；新增 `loop_ledger.md`、`verify-upload.sh`；本行 | 撤两条错判：① "Drive 不通/别发链接"——沙盒 curl 不通 ≠ 工具读不到（对照实验证明）；② "附件已落在 /home/user/uploads"——实测该目录不存在，全盘 find 无匹配；另外我差点把 `/tmp/arena-workspace/coding.patch`（平台临时物）当用户文件收走，已排除 |

| R10 | **你说「我改好了 session5 Drive folder」→ 我重读：7 项 → 14 项**。新增 7 份文件（`确认设计稿-闭合面盖-带Logo字.png` + `111/112/113` + `01_EXP1-A01` + `09_VisualMethod` + `09_AICAD`）；我读完 4 份关键件 | 两条硬收益：① **「先查旧记录」当场兑现** —— `111` 里 **FCStd 已解析完**（db-4/DC-4 几何 + 电池盖包络 + Slider 运动边 + 103–109 产物清单 + Gate 状态；具体数值 R16 已裁出仓库，原文=Drive `111/107`），**我原本要重做的 STL/FCStd 几何解析整轮省掉**；② 他们的 **100 分 QA 评分表**（8 维加权 + 6 红项，≥95 进候选）**替换我上轮自拟的阈值** | `DRIVE_INDEX` 重写 Folder 1 表（14 项全登 FILE_ID + 读状态）+ 新增 §4 CAD 事实 / §5 通道结论；`PRODUCT_TRUTH` 新增 §1ab；`QA_ledger` 改用项目 100 分制 + 新增 K11（银/黑转换需你确认）/ K12（干涉未定不写工程口径）；`REUSABLE` 4 项状态改「已读」 | **open**：109 候选等你批准、历史清理 BLOCKED、干涉 ≈15.18mm³ HOLD、`确认设计稿` PNG 仍未进沙盒 |
| R11 | **自纠（能力归因）**：你问「以前 Drive 链接就够，为什么多一步 push」。我第一轮只答「沙盒拉不到」→ 你追问后我才真跑 `curl -v`：TCP **连上了**（`142.250.99.132:443` → `Connected`）但 TLS 死于 `SSL_ERROR_SYSCALL`，同时 github/npm/PyPI=200；`fetch_page` 对 png/zip 一律 500、对 md 成功；`lh3.googleusercontent` 缩略图也 500 | ① **别把一次工具失败写成规则**，先花 10 秒定位在 TCP/TLS/HTTP 哪一层；② 规则必须区分「**沙盒网络策略**」与「**我的配置**」——上一 session 能 curl 是策略不同，不是我这次做错了什么；③ **你既有通道能用的我继续自己用**（本轮 4 份新 md 就是从 Drive 直读的，零额外动作），只对**二进制**提最小增量步骤 | `capacity.md` 补机制（egress 白名单 + `e2b-ca.crt` TLS 中间人）；`DRIVE_INDEX` §5 六行通道对照表含失败证据 | closed |

| R12 | 五条：①问能否从旧方法论学「旧 session 怎么从 Drive 拿 STL」 ②澄清设计师面盖确认图已在 Drive（别再等聊天附件） ③**109 补投：做错——电池盖该在底部却被贴到上部圆弧面**（session4 未及投诉即容量爆） ④查 session3/4 容量爆根因并立方法论 ⑤要我列 CAD 文件清单；另问 product truth（A01）看没看到 | 新增 `binary-artifact-capacity-discipline`（累计 24）；109→`rejected-by-user` + QA 红项 K13（部件位置以 CAD 坐标系为准，图像方位禁止）；png 取像素再测（view/download 双 500）→ 通道答案写死：文本自读 / 二进制 inbox 或聊天附件 | 新建 `00_meta/REQUEST_FILES.md`（P0/P1/P2 + 格式换算约定）；`capacity.sh` §3 加二进制堆积计数；`CAD_LOG` 新增 §3d；`PRODUCT_TRUTH` §1ab 记 109 判废；`QA_ledger` K13；`DRIVE_INDEX`/`REUSABLE`/`README` 同步 | 更正我上轮一句：「109 等你批准」→ 你其实早已看过且**判废**，只是投诉被容量截断 |

| R13 | 六条：①三张 FreeCAD 截图直接发在消息里（说明「定期删、本地有备份」）②别占 GitHub 空间 ③只留临时空间、结论后删 ④实拍视频在 session3 Drive、要我列抽帧需求 ⑤session5 folder 已更新 ⑥还缺什么文件；当前目标=**铝壳+确认面盖+正确电池盖的成品图** | 方法论 +1：`binary-starve-to-text`（#25）——STEP 222KB 文本可自读 + **OCP 渲染栈在沙盒内打通**（libGL stub×119 符号，配方入 capacity §4c）；截图=历史 image-1/2/3（94/101 登记件），视觉结论与 107/105 交叉一致：盖贴 +Z 端面、凸筋先入后压平、31 遮挡按 A/B 双图规则（K14） | `CAD_LOG` §3e（精确 bbox/体积/placement/两个子文件夹 id/STEP 头）；`PRODUCT_TRUTH` §1ac（电池盖定案+107§6 工序+94 组合唯一）；`QA_ledger` K14；`DRIVE_INDEX` 21 项重登记；`REQUEST_FILES` v2（含抽帧需求表）；`REUSABLE`/`LOOP_AUDIT`/`capacity` 同步 | 更正①：附件「落盘后定期删」→ 实测本沙盒**从不落盘**（第 4 次），无物可删，聊天附件仅肉眼级；更正②：上轮说「渲染要等出网 session」→ 错，stub 自修复后本沙盒就能渲 |

| R14 | 五条：①`CAD_LongTerm_Workspace_2026-09-19.zip` 有没有用 ②`compute_common.py` 有没有用 ③`Asset_Audit` 找不到、是否在删除记录里 ④`SX_DC_GAI_4` 已上传 ⑤查 folder 更新 | ④⑤当场兑现：folder 14→23 项，`sx-dc-gai-4.stp`（17 chunks 文本）+ `102` + `70` + `09_Inventory` 全部读完/登记；**电池盖几何源闭环**（与 107 数字一致）。①有用但**别传 zip 本体**——zip=二进制打不开，要从你本地备份解出 5 份小文本（Keyframe/Asset_Audit/Geometry.json/66/95-96）投 folder；②有用，py=文本，投 folder 我直读（渲染工序母本）。③证实：不在 113 保留清单=已随 09-20 清理删除，只剩两代快照（09-19 你手里那份 zip + 92MB 09-20 快照） | `DRIVE_INDEX`（23 项 + 4 新行含 STEP 登记）；`CAD_LOG` §3e（zip 真相=5 STEP 无 STL、清理安全检 6 步入册）；`PRODUCT_TRUTH`（状态词表对齐 102 §6）；`REQUEST_FILES` v3 | 无撤回；顺带把 `102`/`70` 里的旋钮 4.30:1 细则标为被 O2 裁决覆盖（旧规则已 superseded，不复活） |
| R15 | **你的批评：「能不这么复杂么」——session4 无数动作死磕 CAD，一张正确产品图都没出；现在只要外观图，能提取 CAD 外观尺寸就够；要我反思+summarize 方法+最短路径出图** | 新增方法论 `tiered-output-before-pipeline`（#26）：**出图先分型（Tier 0 提案级 / Tier 1 工程级），级别决定取证深度；完备做法存在≠必须先用**。Tier 0 三步=锁输入（尺寸/版式/材质/禁项全在已入库文本）→ 约束生成 A/B → 剪影 bbox 数值 QA（25° 视角目标比例 0.50，±6%）；两轮不过→降级提问，禁止循环 | 建 `00_meta/SHORTPATH_IMAGES_CN_v1.md`（分型表+6 条不必要动作反思+三步法+对 zip/compute_common/Asset_Audit 按新规则收敛答复）；**当场出图**：v1 两版剪影 0.399/0.376 偏瘦超差→自拒删除；v2 修正 0.488/0.526 ✓、8 要素清单全过、红项 0 → `.scratch/hero_v2_A_logo.png`+`_B_clean.png` 待你审，不进 git；**全程 0 新文件请求、0 GitHub 动作** | **撤销我上轮的「渲染轮」计划**（拉 STEP 全文→网格化→8 视图→100 分 QA 再出图）：那是 session4 死磕模式的重演；该链降级为「仅当你要工程级/规格图时才启用」。另收回对①②的追问：zip/脚本/审计=只对 Tier 1 有用，现在别再动 |

| R16 | 三条：①**CAD 相关全部汇总到一个 md，合并后删老**②只保留外观图相关尺寸（铝壳），内部尺寸全删防干扰 ③重贴确认稿链接：**所有图必须与 `确认设计稿-闭合面盖-带Logo字.png` 比对**，问为什么跑那么远、哪些文件干扰、记录防复发 | 建 **`CAD_LOG.md`=CAD 唯一来源**（§1 外观尺寸/§2 O1–O9 比对表/§3 电池盖外观结论/§4 干扰台账+prompt 禁令/§5 Tier1 指针/§6 历史一览）；新增方法论 `prompt-single-source`（#27）：出图 prompt 只准引用 CAD_LOG §1–§3，引用工程文字=违规 | PRODUCT_TRUTH §1ab→指针（删 face/edge/vertex、bbox、体积、placements、15.18mm³ 等全部内部数）；DRIVE_INDEX §4→指针；REUSABLE 三行改"仅 Tier1"；QA K12 去数字+新增"逐条对确认稿"基准；REQUEST v4（外观阶段零请求，CAD 项全降 Tier1）；v2 图判跑偏→按 §4 修正重生 v3，v2 删除 | 跑偏根因（入 §4 台账）：66/102 旧旋钮规则=凸起把手；A01 氧化黑+31 黑样机=壳色深炭；101/105 matte-frosted=皮革纹；107「4 条加强筋」=喇叭格栅感；我自加 chunky=剪影显胖 |

| R17 | 用户纠正：①「跑道形/两端完整半圆」错了——**只有一端为半圆**，查为什么出错 ②O1 同错，查还有哪些地方同样错 ③以 CAD 外形尺寸+面盖确认稿比对全部信息，错误信息全删 | 根因：「stadium/两端半圆」是**我 R16 合并时未核原文的推断**（重拉 Drive `111/107/94/101` 原文核实：四份都只有包络 39.8×22.5×90.5，无一描述轮廓；且 `QA_ledger` 反复犯错记录里早有「锁形改成跑道形→用户判不能锁定」，我没去查）；**`sx-db-4.stp` 原文复核**：前墙直段 39.8×65.6、圆盘端 CYLINDRICAL_SURFACE **R19.9 完整半圆**、盖端平直面 + 两角 **R4.8** 圆角（TOROIDAL），19.9+65.6+5.0=**90.5 与包络严丝合缝**；电池盖(DC-GAI) 装在平直端（与 109 被否记录一致）→ **上端(圆盘端)=半圆，下端(电池盖端)=平直+小圆角** | 全仓 grep：活跃错误仅 2 处（`CAD_LOG §1` 轮廓行、`QA_ledger` Tier0「跑道壳」要素）→ 均已改正；QA 红项**新增**「画成两端皆圆/跑道形」；`CAD_LOG §2` 恢复 O1–O9 比对表（O1=改正后轮廓，逐图必对）；§4 干扰台账+1 行并补强规则「**外形描述必须能指到原文证据，无证据=不写**」 | v3 按正确轮廓重生成：两图剪影 0.526/0.498（目标 0.50±6%）PASS，肉眼核 O1–O9 逐条过；v2 已删 |
| R18 | **旋钮又错** → 旋钮相关历史全部汇总到一个文档 + 删除错误旋钮信息 | 重拉 `102` 原文（§3.C）核实：旋钮硬规则「近直角矩形长条/≈4.30:1/接近90°端部/小实际边缘半径/普通塑料/无金属套·装饰环·胶囊端·槽·齿·十字纹/位置方向比例=确认稿」仍在**「唯一允许使用：66」**之内，「66 只决定旋钮」；根因=R14/R16 我把该**有效**规格误标「被 O2 裁决覆盖/作废」（O2 否的只是 v1/v2「凸起把手」画法），v3 prompt 又写「rounded short ends」(胶囊端)+条偏粗(≈3:1)+明显浅棕嵌件色 | 建 **`KNOB_LOG.md`=旋钮唯一来源**（§1 正确规格/§2 历史与错误台账/§3 指针）；7 文件错误旋钮信息删除改指针：CAD_LOG §1/§2/§3/§4、PRODUCT_TRUTH（superseded 列表+O2+§1 表）、QA_ledger（K5+反复犯错记录）、REUSABLE 2 行、DRIVE_INDEX 2 行、REQUEST_FILES 1 行、methodology #27 例证 | v4 按 KNOB_LOG §1 重生成（近直角/4.3:1/近直角端/同材质稍浅/齐平微凹），v3 删除 |
| R19 | 重发三条 + 附两张图：`104`（**旋钮+面板=都对**，唯独底部电池盒筋没有）+ `确认设计稿` png（像素级参考） | 附件再次未落盘（`/home/user/uploads` 不存在，实测）；按内联图像素级参考取证：**灰盘 + 浅米白/乳白近直角条**（v4「同材质稍浅」错）、确认稿电池盒区**有筋**（细浅横纹）、原稿混 CAD 坐标轴（O7 再证） | 重写 `KNOB_LOG` §1/§2（104=旋钮/面板正确性基准；颜色=浅米白+灰盘；v4 颜色错入台账）；`CAD_LOG` §1/§2 O2/O8（筋必须画）/§4 更新；QA Tier0 8 要素改「灰盘+浅米白斜条」+「底部 3 道筋」 | v5 按新规格重生成（剪影 0.516/0.497 PASS；A 版肉眼逐条：灰盘/浅米白条/近直角端/3 道筋/平直底全过）；**R20 用户叫停出图线，v5 留 `.scratch` 候选** |
| R20 | 停止出图线，转三项制度任务：①以前方法论的**所有禁编造限制**汇总到一个文档 ②**以后所有新 session 都要有系统顶层**：loop、空间、命名体系、限制编造 ③所有**产品 truth 限制**加进产品汇总+CAD 汇总 | 建 **`NO_INVENT.md` 禁编造约束总集**（N0–N3 总纲 + 外形/数字/声称/流程四类 + 变更规则；来源=methodology 27 条 + 102 §7 不可变禁令 + StartHere 禁止 + PRODUCT_TRUTH §3 + QA 红项 + 用户永久指令，逐条带出处）；建 **`SESSION_BOOTSTRAP.md` 系统顶层**（开局五步 + loop=M2/M9 轮内节奏 + 空间=5 处纪律表 + 命名体系+单一来源文档表 + 禁编造三条铁律） | `PRODUCT_TRUTH` 加 §7 产品红线 P1–P10；`CAD_LOG` 加 §6 CAD 红线 C1–C6（均为一行版+指针 NO_INVENT，不复制全集）；README 开局顺序接线 SESSION_BOOTSTRAP（新 session 第一页） | 无（本轮纯制度）；单一来源文档扩为 4 份（PRODUCT_TRUTH/CAD_LOG/KNOB_LOG/NO_INVENT） |
| R21 | ①停止并取消前面任务（出图线）②旋钮历史汇总到一个文档+错误旋钮记录全删 ③比对两附图的生成记录/旋钮记录，**只保留这 2 个旋钮记录为正确的** | 附件仍未落盘（`/home/user/uploads` 不存在，实测）；按内联图取证：两图旋钮**一致**（灰盘/浅米白近直角长条/近直角端/斜向≈60°/扁平）→ 互证；104 生成记录=102 闸门图 2（105/111 状态"可审查"→用户 R21 升级为"正确"；深壳不采纳），确认稿=设计师 CAD 渲染（含坐标轴→O7 清除） | 重写 `KNOB_LOG` R21 版：§1 唯一正确=两图（含 104 面板登记+电池盒筋）/§2 删除清单/§3 一行审计；66/70/102 文字规格按 R21 从"有效"改判**不再作依据**——7 文件同步（CAD_LOG §1/§2/§4、PRODUCT_TRUTH §0/O2/§1 表、QA_ledger K5+反复犯错、REUSABLE 2 行、DRIVE_INDEX 2 行、REQUEST_FILES 1 行） | v3/v4 图（判错）删除；v5 留 `.scratch` 未交付候选；**出图线取消，待你下一条出图指令** |
| S10-R1 | 新 session 只做接管，不继承旧完成声明，不扫描 | 事后补记。当时按指令未读文件、未改仓库 | 无 | 无 |
| S10-R2 | 先学 GTM / 品牌 / 网站计划，要资料缺口和到网站的最短时间 | 事后补记。读了仓库交接和三份 Drive 计划正文。小时数为计划，不是实测 | 当时未写入台账 | 不把未读的 12 天拆解写成已读 |
| S10-R3 | 总文件夹只建文件名索引；仓库索引单独摘要；合并后全部待筛选 | 事后补记。目录可读，约 85 个文件名。未下载产品文件 | 当时未写入台账 | 不把同名文件当成同一份 |
| S10-R4 | 要求补上消息台账 loop 和方法论 loop，筛选、总结、瘦身，开始沉淀 Session 10 | 与 S10-R5 一并落地，避免写两套卡 | 见 S10-R5 | 无 |
| S10-R5 | 从 Drive 与仓库文件名里挑有用的，学习后瘦成新方法，不重造轮子 | 新增 6 条流程方法；有用文件与冲突收成一页 | `session10_method.md`；`methodology.md` §E；本台账；`session_log.md`；`LOOP_AUDIT.md`；README / HANDOFF / PRODUCT_TRUTH 只加待筛选横幅 | 不把目录草稿的 Validated 和工程目标升格为产品事实；不抄 Drive 标识进 Git |
## 我此刻对你消息里那 3 个具体问题的答复去向
- 「上传的 session3 md」→ 沙盒里不存在（`/home/user/uploads/` 无此目录，全盘 `find` 无匹配）→ 已把"别信 UI 成功标签"写成方法 `verify-artifact-not-status-label`，并配 `verify-upload.sh`。
- 「Drive 是不是更好」→ 不是 Drive 更好，是**我之前判断错了**；两条通道都可用，Drive 直链成为你的默认通道（M8b）。
- 「每条 message 是否进了 loop」→ R1–R3 现在都有台账；R1/R2 是我**事后补记**（当时没有台账这个机制，这正是漏洞），从 R3 起是即时登记。
