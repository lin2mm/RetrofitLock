# INDEX — Session 1–4 资料总索引（给 Session 5 查，不用重复劳动）

怎么用：
1. 你只看**「状态」列**为 `❓待你上传` 的行，按「送达方式」把文件丢进聊天即可（一条消息发一组同 ID 的）。
2. 我拿到后会：归档进 `90_archive/<session>/` → 派生进 `10_product/`、`40_images/refs/` → 把该行状态改成 `✅已入库(路径)`。
3. 标 `⚪不用给` 的**别花时间找**，我这边已有等价物或不影响第 ①②③ 步。
4. 送达方式（实测）：**Drive 链接我打不开**（Drive/Figma/Dropbox/各 CDN 在本沙盒不通）。
   可行：① 聊天附件；② 让源 session `git push` 到 `lin2mm/RetrofitLock` 某分支，我 fetch（大批量首选）；
   ③ 视频先抽帧（`ffmpeg -i v.mp4 -vf "fps=1,scale=1600:-1" f_%03d.jpg`，20–30 张够）；
   ④ `.ai/.psd/.blend/.max` 源文件不用传（我没这些软件），传导出图（PNG ≥1600px，最好带透明底）；
   ⑤ >25MB 单文件一律不进 git（GitHub 会拒），只传缩略/抽帧版。

状态图例：`✅沙盒已有` · `❓待你上传` · `⚪不用给` · `🔶只缺关键片段`

---

## Session 1 — 智能锁海外市场 + 爬取数据

| ID | 资料 | 我的用途（Step） | 状态 | 备注 |
|----|------|------------------|------|------|
| S1-A | 市场研究结论文档（趋势/机会/价格带） | 只用于校准目录卖点排序（②） | ⚪不用给 | 结论已固化进 ICP 与话术；给我一句话摘要即可 |
| S1-B | 竞品清单（品牌/型号/价格/卖点） | 做 `comparison` 对比图、目录"为什么选我们"页（②③） | 🔶只缺关键片段 | **只要 5–8 个直接竞品那一小段**，不要整份爬取结果 |
| S1-C | 原始爬取数据集（几千条 + 字段说明） | 暂不用 | ⚪不用给 | 体积大、会撑爆仓库（`capacity.md` 红线）；要做价格带图时我再单独找你要 |
| S1-D | 爬取脚本 / 数据字典 | 复用方法（未来再爬时） | ⚪不用给 | 不进本次三步流程 |
| S1-E | 目标国家/地区优先级结论 | 决定目录要不要分美规/欧规/中东版本（③④） | ❓待你上传 | 一页摘要就够 |

## Session 2 — Methods 总结 + ICP

| ID | 资料 | 我的用途（Step） | 状态 | 备注 |
|----|------|------------------|------|------|
| S2-A | **ICP 定义**（installer / dealer / property mgr… 规模、决策人、采购节奏） | 目录主线"按 ICP 选配件包"、封面大字、`icp_variant` 图（①②③） | ❓待你上传 | **阻塞项**，最小可开工组合之一 |
| S2-B | Methods 总结（获客方法论正文） | 校对我 `methodology.md` 里 B 组三条反推的措辞（长期 loop） | ❓待你上传 | 有了它我才不改错你的方法 |
| S2-C | 卖点优先级排序 | 图上标注与页面顺序（②③） | ❓待你上传 | 若藏在 S2-A 里，给 S2-A 就够 |
| S2-D | 不该卖给谁 / 排除条件 | 兼容表里"不适用"栏，避免售后争议（③） | ❓待你上传 | 一句话级别也行 |

## Session 3 — 客户数据 → DM → 索目录 → 渲染图 + 目录（上一 session）

| ID | 资料 | 我的用途（Step） | 状态 | 备注 |
|----|------|------------------|------|------|
| S3-A | **成品设计稿**（外观定稿图/渲染，PNG 导出即可） | 图生图的参考输入，锁造型与光线（②） | ❓待你上传 | **阻塞项**；源文件不用给 |
| S3-B | **实拍半成品视频**（产线/装配/试装） | 抽帧 → 真实几何、装配件关系、质感（②） | ❓待你上传 | **阻塞项**；抽帧或直接传都行 |
| S3-C | **配件 / 适配板清单**（适配哪种锁体、装法、工具、耗时） | `10_product/accessories.csv` 覆盖我猜的 6 行；兼容矩阵（①③） | ❓待你上传 | **阻塞项**，没有它"系列"就是空的 |
| S3-D | 已发出的目录版本（PDF/截图/HTML 任一） | 复刻版式、字号、页序（③） | ❓待你上传 | 有了它第③步是重排而非重新设计 |
| S3-E | DM 话术原文 + 数据（发送/回复/索目录数） | 判断目录页数与承接句（③） | ❓待你上传 | 回复率最高那句 = 封面大字 |
| S3-F | 客户提问/异议原话（英文，别翻译） | 目录 FAQ + 图上标注（②③） | ❓待你上传 | 按主题分类更好，乱序我也能归 |
| S3-G | 旧渲染图成品（PNG/JPG，若与设计稿不同批） | 同系列延展的底图（②） | ❓待你上传 | 有多少给多少，我先挑清晰的 |
| S3-H | 客户名单（国家/类型/状态） | 语言与版本优先级、按地区适配表（③④） | 🔶只缺关键片段 | 给我聚合数字 + 3–5 个典型样本，**别传客户隐私全表** |
| S3-I | 目录制作用的排版稿（Figma/AI 链接） | 精确复刻栅格 | ⚪不用给 | 我打不开外部设计工具；导出 PDF/截图即可 |
| S3-J | 产品规格表（尺寸/孔距/门厚/认证/供电） | `base_unit.md` 定稿（①） | ❓待你上传 | **阻塞项**；没有它就做不了 `dims_annotated` |

## Session 4 — GlobalLockSummary 工程师资料库

| ID | 资料 | 我的用途（Step） | 状态 | 备注 |
|----|------|------------------|------|------|
| S4-A | 站点内容清单（有哪些板块、给工程师讲什么） | 判断目录/网站能否共用信息架构（④） | ❓待你上传 | 一页大纲就够 |
| S4-B | 源码（zip / 或让那 session push 分支） | Step ④ 直接复用技术栈与组件 | ⚪不用给（现在） | **做到第④步再传**，否则只是占空间 |
| S4-C | 线上 URL（若已部署） | 我直接看结构与缺口 | ❓待你上传 | 只需文字 URL，我能用 `fetch_page` 打开公开页 |
| S4-D | 网站用途定位：工程师学习 vs 客户获客 | 决定第④步是扩建还是新做（④） | ❓待你上传 | 这是决策不是资料，你回一句话就行 |
| S4-E | 已写的技术说明文（锁体/孔距/装法解释） | 目录技术页与尺寸图注解复用（②③） | 🔶只缺关键片段 | 只要"装法与兼容性"相关段落 |

## 我（Session 5）已经有的，你不用再造

| 资产 | 路径 | 状态 |
|------|------|------|
| 顶层规则 / 命名体系 / 方法论库 / 容量红线 / 自检脚本 | `00_meta/` | ✅沙盒已有（本 session 建立，已 push） |
| 项目进度与交接 | `00_handoff/HANDOFF.md`、`session_history.md`、`session_log.md` | ✅ |
| 上传清单（按优先级的精简版） | `00_handoff/assets_index.md` | ✅（与本页互补：本页是**全量索引**，那份是**先给什么**） |
| 主机事实卡模板 + 配件矩阵模板（含我猜的占位行） | `10_product/base_unit.md`、`accessories.csv` | ✅ 等 S3-C/S3-J 覆盖 |
| 图位清单 11 条（已按"主机渲染一次"重排） | `40_images/slots.csv` | ✅ 等参考图解阻塞 |
| 风格基线图 2 张（虚构造型，禁外发） | `40_images/out/PROOF-STYLE_*` | ✅ 参考图到齐即作废 |
| 中文字体（npm）、Pillow/fpdf2/fonttools、static ffmpeg | 沙盒内（`~/.npm-fonts`、`~/.local`） | ✅ 本 session 有效；下个 session 用 `--fix` 重装 |

---

## 阻塞判定（一句话）
现在**卡住第 ① 步的只有 4 件**：`S3-A 设计稿`、`S3-B 视频帧`、`S3-C 配件清单`、`S3-J 规格表`；
卡住文案与页面顺序的是 `S2-A ICP`；卡住封面大字的是 `S3-E` + `S3-F`。
其余都不阻塞，可以边做边补。
