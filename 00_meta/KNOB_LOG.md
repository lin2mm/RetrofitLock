# KNOB_LOG — 旋钮唯一文档（2026-09-20 R21：用户裁定——只有两张附图的旋钮记录是正确的）

> **用户 R21 原话**：「历史记录与旋钮有关的都汇总到一个文档，与旋钮有关的错误记录全部删除；
> 比对此附件 2 个图片的生成记录、旋钮记录，**只保留这 2 个旋钮记录为正确的**。」
> 本页 = 旋钮的唯一文档。唯一正确依据 = §1 的两张图；
> 其余旋钮文字比例已删除，不再复述。出图只认下面两张确认图。

## 1. 唯一正确的旋钮记录（仅这 2 个；出图 prompt 只准引用本节）

### 图 A：104 图（用户确认：旋钮+面板都正确）
- 文件：`104_GTM_From31_LeftFront25_ConfirmedCover_Knob_BatteryCover_Image2_Confirmation.png`（用户 2 次附图，R19/R21）
- 生成记录：102 双图闸门「图 2」（31 左前 25° 相机 + 确认面盖 + 确认旋钮）；`105/111` 状态=面盖/旋钮视觉候选可审查 → **用户 R21 升级为"正确"**。
  注意：104 的深色壳是 31 时代产物，**不采纳**（壳色按 `CAD_LOG §1` = 浅银灰铝）；电池盒区 104 漏筋（筋按图 B）。
- 图 A 的旋钮（= 正确形态）：
  - **灰盘**：顶部大圆，近满宽随顶弧；中灰哑光；齐平/微凹；一圈细深边缘线
  - **旋钮条**：**浅米白/乳白**；近直角矩形、细长（长≈宽 4 倍）；端部近直角（小半径，**非胶囊端**）；
    扁平（微倒角，**非凸起把手**）；无槽/齿/金属套；**斜向（左下→右上，陡斜约 60°）**；近盘中心
- 图 A 的面板（用户确认正确，一并登记）：棕红细密纹理面盖 / 盘下小灰点（LED）/ 中部棕色圆形功能键 / LOGO 位

### 图 B：确认设计稿（设计师稿，外观唯一底稿）
- 文件：`确认设计稿-闭合面盖-带Logo字.png`（用户 2 次附图，R19/R21）
- 生成记录：设计师 CAD 渲染（面盖视角，斜 45°）；原稿混入 CAD 坐标轴（红/绿/蓝）→ **O7：一切使用须清除**
- 图 B 的旋钮（= 正确形态）：与图 A 同——灰盘 + 浅米白近直角矩形扁平条（斜向、近直角端）
- 图 B 其余相关：底部电池盒区**有细浅横纹筋**（3 道，O8；104 漏了 → 必须画）

### A × B 比对结论
两图旋钮**一致**（灰盘 / 浅米白条 / 近直角矩形 / 近直角端 / 斜向约 60° / 扁平）→ 互证。
**这 2 个旋钮记录 = 唯一正确的旋钮记录**；出新图只按本节自检，不再引任何文字规格。

## 2. 已删除的错误/失效旋钮记录（全部）
- 旧旋钮文字比例已删除，不复述。只认上面两张确认图
- 我的历次旋钮措辞错误（见 §3）：已从 `CAD_LOG`/`PRODUCT_TRUTH`/`QA_ledger`/`REUSABLE`/`DRIVE_INDEX`/`REQUEST_FILES`/`methodology` 全部删除，改指本页
- 生成失败图 v1/v2/v3/v4 的旋钮：错误，已作废（文件在 `.scratch` 判删）

## 3. 错误审计（只留行，不还原任何内容；不删除审计本身=102 规则）
| 轮次 | 旋钮错法 | 状态 |
|---|---|---|
| v1/v2（09-20） | 同色**凸起把手** | 用户否决，记录已删 |
| v3（R17 轮） | 胶囊端 + 条偏粗 + 焦糖棕 | 用户判错，记录已删 |
| v4（R18 轮） | 条渲"同材质稍浅棕" + 盘渲棕色 | 与图 A/B 不符，记录已删 |
| 我 R14–R19 记录 | 「规格作废/superseded」误判、"同材质色差"误色 | 已删（R18–R21 三轮清理） |

## 4. 指针
- 两图文件登记 / Drive id：`DRIVE_INDEX.md`；出图线 R21 起**取消**（v5 为未交付候选，留 `.scratch` 待下次出图指令）。
- 其它外观项红线：`CAD_LOG.md` §6 / `NO_INVENT.md`。
