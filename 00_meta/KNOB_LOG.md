# KNOB_LOG — 旋钮唯一汇总（2026-09-20 R18 建档 / R19 按用户附图更正颜色）

> 用户 R18 指令：旋钮又错 → **所有旋钮相关历史汇总到本文件**，错误旋钮信息删除。
> R19：用户附上 `104` 图 + `确认设计稿` png 作参考——**104 的旋钮+面板 = 用户确认正确**（唯独底部电池盒筋没有）。
> `CAD_LOG`/`PRODUCT_TRUTH`/`QA_ledger`/`REUSABLE`/`DRIVE_INDEX`/`REQUEST_FILES`/`methodology` 里的旋钮行已全部改为指向本文件；
> 凡「4.30:1 规则作废 / superseded」的记录均为**错误信息，已删**。

## 1. 旋钮正确规格（出图 prompt 唯一允许引用的旋钮数据）
权威源 = 用户确认的 **104 图**（`104_GTM_From31_LeftFront25_ConfirmedCover_Knob_BatteryCover_Image2_Confirmation.png`，
本轮用户附图：旋钮+面板正确）+ `确认设计稿-闭合面盖-带Logo字.png`（本轮用户附图）
+ `66` 裁剪/`102 §3.C`/`70`/`StartHere` 硬规则（原文复核一致）+ 用户 O2 终审（功能级）。

```text
圆盘区    顶部大圆盘 = **灰色**圆形机构区（中灰，与棕色面盖明显不同色；微凹/齐平）——104 与确认稿均为灰盘
旋钮条    颜色 = **浅米白/乳白**（明显浅于盘与盖，清晰对比；不是棕色同材质色差！
          v3 焦糖色、v4 "同材质稍浅" 均错，R19 按 104+确认稿更正）
          形状 = 近直角矩形长条，长宽视觉比例 ≈ 4.30:1（细长）
          端部 = 接近 90°（小实际边缘倒角）；胶囊端/圆头端禁止
          姿态 = 扁平/微凹于盘面；无槽、无齿、无十字纹、无额外间隙
          位置/方向 = 盘内斜向（条自左下→右上，约 60° 陡斜，近盘中心）——按 104（用户确认正确）
附件      **无**金属套、无装饰环、无垫片（K5 红线）
禁止引用  v4 旋钮资产（v4 裁剪/网格/角度图已删除，102/96 禁令）；「凸起把手」画法（v1/v2 错误）
          注：104 的深色壳是 31 时代产物，**不采纳**；壳=浅银灰铝（见 CAD_LOG §1）
```

## 2. 历史与错误台账（旋钮每一代为什么错）
| 轮次/来源 | 记录 | 状态 |
|---|---|---|
| `66` 裁剪 + `66/70/StartHere/102`（09-19~09-20 冻结） | 近直角矩形长条 / ≈4.30:1 / 接近 90° 端部 / 小实际边缘半径 / 无胶囊端、金属套、装饰环、槽、齿、十字纹 / 普通塑料 / 位置·方向·相对比例=确认稿 | ✅ **唯一有效规格**（R14/R16 曾误记为「作废/superseded」——R18 重拉 `102` 原文核实：硬规则仍在「唯一允许使用」内） |
| 用户 O2 终审（R9/R16） | 盘内斜向条 = 手动旋钮区 | ✅ 有效（功能级确认；**否的是「凸起把手」画法，不是比例/端部规格**） |
| **104 图（R19 用户附图）** | 旋钮+面板=用户确认**正确**（灰盘+浅米白条+棕盖布局）；唯独底部电池盒筋没有 | ✅ **旋钮/面板正确性基准图**（像素附件未落盘，按肉眼；壳色不采纳） |
| **确认设计稿 png（R19 用户附图）** | 灰盘+浅米白近直角条；底部电池盒区**有筋**（细浅横纹）；原稿混入 CAD 坐标轴（O7 清除） | ✅ 比对权威（像素级对齐本轮起可按附件肉眼+裁剪） |
| v1/v2 渲染（09-20） | 旋钮=同色**凸起把手** | ❌ 用户否决（R16 根因台账） |
| 我的 R14/R16 记录 | 「4.30:1 细则被 O2 裁决覆盖、规则作废、不复活」 | ❌ **错误信息，已删**（散布 7 文件，R18 全部清成指针） |
| v3 prompt（R17 轮） | 「rounded short ends」(胶囊端)+条偏粗(≈3:1)+焦糖棕嵌件色 | ❌ 用户判「旋钮错误」（R18） |
| v4 prompt（R18 轮） | 条渲成「同材质稍浅棕」+盘渲成棕色 | ❌ 与 104/确认稿不符（应为**浅米白+灰盘**），R19 更正 |
| `96_V4_Knob_Error_Cleanup_Manifest` | v4 旋钮错误清理清单 | v4 资产禁止引用（禁令保留） |

## 3. 指针
- 文件 id（66 裁剪 / 70 / StartHere / 102 / 96 / 确认稿 png / 104）：见 `DRIVE_INDEX.md`（不在本文件重复）。
- 其它外观项干扰台账（壳轮廓/壳色/纹理/电池盖）：`CAD_LOG.md` §4。
- 出图产物：`.scratch/hero_v5_A_logo.png` / `hero_v5_B_clean.png`（R19 按本文件 §1 + CAD_LOG §1 重生成）。
